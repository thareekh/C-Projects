void comment_removal(char * sf,char * df)
{
    int ch;
    char p[50];
    while((ch=fgetc(sf))!=EOF)
            {
                p[i]=ch;
                i++;
                if(ch=='\')
                {
                    

            }
}

